const SCHEDULE_TYPE = Object.freeze({
  ONLINE: 'Online',
  OFFLINE: 'Offline',
});

const SCHEDULE_SESSION = Object.freeze({
  MORNING: 'Morning',
  AFTERNOON: 'Afternoon',
});

module.exports = {
  SCHEDULE_TYPE,
  SCHEDULE_SESSION,
};
